# Auto detect text files and handle line endings
* text=auto

# Force LF for shell scripts and config files used inside Linux containers
*.sh text eol=lf
*.env text eol=lf
.env.example text eol=lf

# Windows batch files can keep CRLF
*.bat text eol=crlf
*.cmd text eol=crlf
